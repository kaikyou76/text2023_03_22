SELECT
    company_cd,
    department_cd
FROM
    tmp_integratedid_department
WHERE
    (company_cd, department_cd) NOT IN (
        SELECT
            company_cd,
            LPAD(section_cd, 5, '0')
        FROM
            mst_section
    );

