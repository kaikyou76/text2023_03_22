もちろん、日本語として自然に読める形に“翻訳”します。
意味が分からない部分を省き、全体をそのまま日本語で理解できる形に整えました。

---

# 🇯🇵 **日本語訳（読みやすく自然な文章）**

以下は、まず WinSCP の接続タイムアウトを 60 秒に延長し、それでもダメな場合に分段階で接続トラブルを調査するための手順書になります。

---

## **第一歩：WinSCP の接続タイムアウトを 60 秒に設定する**

1. WinSCP を開き、「セッション」画面で「詳細」ボタンをクリック（ログインの隣）
2. 左側メニューから「接続」→「タイムアウト」を開く
3. 「サーバー応答タイムアウト」を *デフォルトの 15 秒 → 60 秒* に変更
4. 「OK」を押して保存し、セッション画面で「ログイン」を押して再接続を試す

※ それでも接続できない場合は、次の工程に進みます。

---

## **第二歩：接続問題の原因を順番に切り分ける（優先度順）**

---

### **1. 基本的なネットワーク・代理設定の確認（最も可能性が高い）**

#### **① 代理サーバー（プロキシ）の接続確認**

1. Windows の「コマンドプロンプト」を開く（Win+R →「cmd」）
2. 下記コマンドを実行（proxy.company.com や 8080 は実際の値に置き換える）

```
telnet proxy.company.com 8080
```

**判定：**

* **正常**：
  「接続しています…」と表示され、画面が空白になる → プロキシは正常

* **異常**：
  「接続できません」と表示 → アドレスが違う or プロキシが停止中
  → 同僚に設定を確認すべき

---

#### **② WinSCP のプロキシ設定が同僚と一致しているか確認**

WinSCP →「オプション」→「プロキシ」

確認すべき項目：

| 項目       | 確認内容                                |
| -------- | ----------------------------------- |
| プロキシの種類  | 同僚と完全一致（HTTP / SOCKS5 など）           |
| アドレス・ポート | 例：proxy.corp.com:8080               |
| 認証       | 必要なら「ユーザー名 / パスワード」が正しいか（ドメイン形式も注意） |

---

### **2. AWS サーバー側の設定確認（同僚の協力が必要）**

#### **① EC2 インスタンスの状態チェック**

AWSコンソールで同僚に確認してもらう：

* 状態が「running」
* 「ステータスチェック」＝ 2/2 OK
* 公開 IPv4 が WinSCP のホスト名と完全一致
  → 再起動で IP が変わることがあるので注意

#### **② セキュリティグループの 22 番ポートが正しい発信元 IP を受け入れているか**

* **正しい例：** 会社プロキシのグローバルIP（例：123.45.67.89/32）
* **誤り例：** 0.0.0.0/0 や 自分のローカルIP
  （会社ネットワークでは直接接続を禁止している場合が多い）

---

### **3. 秘密鍵ファイル（.ppk）と WinSCP 設定の詳細確認**

#### **① .ppk ファイルの有効性**

* 同僚のファイルと **サイズが一致する** か
* テキストで開いたとき

  * 先頭：`PuTTY-User-Key-File-2: ssh-rsa`
  * 最後：`---- END SSH2 PUBLIC KEY ----`
    → どちらか欠けていたら破損

#### **② WinSCP の高度な設定**

* IPv4強制：
  「接続」→「TCP/IP」→「IPv4 だけ使用」にチェック
* SSH バージョン：
  「SSH」→「優先プロトコル」→「SSH-2」
* ログ記録：
  「ログ記録」をオンにし、失敗ログで原因を特定
  例：

  * *Timeout waiting for SSH banner* → ネットワーク/プロキシ問題
  * *Host is not communicating* → サーバーに到達していない（セキュリティグループ等）

---

### **4. ローカルPCの環境問題の切り分け**

#### **① 防火壁・VPN の一時停止**

* Windows Defender Firewall を一時的に OFF にする
* VPN を切ってプロキシだけで接続を試す
  （VPN＋プロキシが衝突する企業もある）

#### **② PuTTY での再検証（推奨）**

* WinSCP と同じ設定で PuTTY をセット
* 同じエラーならネットワーク問題
* 違うエラーなら鍵 or AWS設定問題

---

## **第三歩：最終確認（ここまでやれば原因はほぼ特定できる）**

1. **同僚のPCであなたの .ppk だけ使って接続を試す**
   　→ 成功なら、あなたのPC側の問題確定

2. **自宅 or スマホテザリングで試す**
   　→ 接続できるなら、会社のネットワーク/プロキシ設定が原因

---

## ❗結論

この手順を実施すれば、**接続タイムアウトの 90% は“プロキシ設定”か“セキュリティグループの誤設定”であることが判明します**。

必要であれば、「あなたの環境で実際にどれを先に試すべきか」優先順位を整理して差し上げます。

以下是先调整超时时间至60秒，再分步骤排查连接问题的完整操作指南：

第一步：将WinSCP连接超时时间调整为60秒

打开WinSCP，在"会话"窗口点击 "高级" 按钮（位于"登录"按钮旁）
在左侧导航栏依次展开 "连接" → "超时"
找到 "服务器响应超时" 选项，将默认的 15秒 修改为 60秒
点击 "确定" 保存设置，返回会话窗口点击 "登录" 尝试连接
若仍超时失败，继续以下排查步骤

第二步：系统排查连接问题（按优先级排序）

一、基础网络与代理验证（最可能原因）

1. 测试代理服务器连通性

操作：打开Windows命令提示符（Win+R输入cmd），执行以下命令（替换为实际代理信息）：
bash
# 测试代理服务器是否可达（以HTTP代理为例）
telnet proxy.company.com 8080


结果判断：

✅ 若显示"正在连接到..."并进入空白界面 → 代理服务器正常

❌ 若提示"无法打开连接" → 代理地址/端口错误或代理服务未启动，需与同事核对

2. 确认WinSCP代理配置与同事完全一致

打开WinSCP → "选项" → "代理" ，逐项核对：
表格
复制
配置项	需确认内容
代理类型	必须与同事相同（如"HTTP"、"SOCKS5"，选错类型会直接导致超时）
代理主机名/端口	例如proxy.corp.com:8080，注意区分大小写（部分代理服务器对主机名大小写敏感）
代理认证	若同事代理需要账号密码，确保"代理用户名/密码"正确填写（区分域账号格式如DOMAIN\user）

二、AWS服务器端配置验证（需同事协助）

1. 确认实例公网IP和状态

同事操作：登录AWS EC2控制台，检查目标实例：
状态："运行中"且"状态检查"显示"2/2 检查通过"
公有IPv4地址：与你WinSCP中填写的"主机名"完全一致（若实例重启过，IP可能已变更）

2. 安全组入站规则必须包含代理出口IP

同事操作：查看实例关联的安全组，确认入站规则中22端口的"来源" 为：

✅ 正确：代理服务器的公网IP（可让IT部门提供，格式如123.45.67.89/32）

❌ 错误：0.0.0.0/0（公司网络可能禁止直接访问公网IP）或你的本地IP（通过代理时无效）

三、密钥文件与WinSCP配置深度检查

1. 验证.ppk密钥文件有效性

文件完整性：右键.ppk文件 → "属性"，对比同事的文件大小（字节数需完全一致，排除拷贝损坏）
格式验证：用记事本打开.ppk文件，开头应为PuTTY-User-Key-File-2: ssh-rsa，结尾为---- END SSH2 PUBLIC KEY ----，缺失则文件损坏需重新拷贝

2. WinSCP高级连接设置

强制使用IPv4："高级"→"连接"→"TCP/IP"，勾选"仅使用IPv4"（避免IPv6解析问题）
SSH协议版本："SSH"→"首选SSH协议版本"，选择"SSH-2"（部分旧服务器不支持SSH-1）
日志记录排查："高级"→"日志记录"，选择"记录到文件"并保存到桌面，连接失败后查看日志，关键错误如：
Timeout waiting for SSH banner → 网络/代理问题
Host is not communicating for 60 seconds → 服务器未收到连接请求（安全组或代理拦截）

四、本地网络环境排除（独立验证）

1. 关闭本地防火墙和VPN

临时关闭Windows Defender防火墙（控制面板→系统和安全→Windows Defender防火墙→"关闭防火墙"）
若使用公司VPN，尝试断开VPN后仅通过代理连接（部分公司网络同时启用VPN和代理会导致冲突）

2. 用PuTTY工具交叉验证（推荐）

下载PuTTY，配置：
主机名：AWS公网IP，端口22
连接→代理：与WinSCP相同配置
SSH→Auth：浏览选择.ppk密钥文件
点击"打开"，若提示Network error: Connection timed out → 问题与WinSCP一致，聚焦网络/代理排查；若提示其他错误（如密钥拒绝），则为密钥或服务器配置问题

第三步：若以上均正常，执行终极测试

同事电脑直连测试：用你的.ppk文件在同事电脑上连接，若成功→问题在你本地环境（如代理配置错误）
替换网络环境：用手机热点（关闭代理）连接，同时让同事将安全组22端口来源临时改为0.0.0.0/0，若能连接→公司代理或网络策略限制

按此流程操作，90%的超时问题可定位到代理配置或安全组来源IP错误。需要我针对某一步提供截图示例吗？
