以下は、**本番MySQLを絶対に壊さず、安全にAWS → ローカルへ4.6GBの巨大DBをコピーするための手順**を、
**失敗しないための注意点つきで**まとめたものです。
（あなたの環境：Windowsローカル、MySQL・Workbench・Tera Term・WinSCPあり）

---

# ✅ 全体の安全コンセプト

* **AWS側では一切「書き込み」「変更」しない。読み取り専用で dump だけ取る。**
* **ローカル側で新しい空DBを作って復元する。上書きは絶対にしない。**
* 4.6GBは大容量なので、**Dump → WinSCP → 復元**の順番を必ず守る。

---

# ✅ 手順（絶対に安全な方法）

---

# **① AWS の MySQL に「読み取り専用」でログインする**

※ TeraTerm で直接 root 権限ログインは危険
→ **読み取り専用アカウント**を使うのが最安全です。

もしなければ同僚に↓作ってもらうのがベスト：

```sql
CREATE USER 'dump_user'@'%' IDENTIFIED BY 'xxxx';
GRANT SELECT, SHOW VIEW, EVENT, TRIGGER ON idra.* TO 'dump_user'@'%';
FLUSH PRIVILEGES;
```

もうこれで本番を壊す可能性はゼロに近いです。

---

# **② Tera Term で AWS にログインして mysqldump を実行**

AWS側でdumpする（4.6GBあるため、圧縮も推奨）

### ▼タイプA（非圧縮：最も安全、確実）

※容量そのまま → WinSCP ダウンロードに時間がかかる

```
mysqldump -u dump_user -p --single-transaction --quick --routines --triggers idra > idra.sql
```

### ▼タイプB（超推奨：圧縮）

4.6GB → 1GB以下になることが多い、転送安定

```
mysqldump -u dump_user -p --single-transaction --quick --routines --triggers idra | gzip > idra.sql.gz
```

👉 gzip を使っても MySQL の中身は変わらない。
👉 本番DBに書き込みは一切しない。

---

# **③ WinSCP で AWS → ローカルへダウンロード**

WinSCP でサーバの `/home/ec2-user/` などにできた：

* `idra.sql`
* または `idra.sql.gz`

をローカルへコピーするだけ。

※ **この操作は100%の読み取り操作**
→ 本番には絶対影響しない。

---

# **④ ローカル Windows で 新しい空DB を作る（上書き禁止）**

Workbench か MySQL コマンドで作成：

```
CREATE DATABASE idra_local CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
```

AWS 本番と **同じ文字コード** にすることが重要。

👉 ※本番のCHARSET/Collateは `SHOW VARIABLES LIKE 'char%';` で確認できます。

---

# **⑤ SQL をローカルに取り込む（復元）**

### ▼圧縮していない場合:

コマンドプロンプト：

```
mysql -u root -p idra_local < idra.sql
```

### ▼gzip の場合（推奨）

```
gunzip idra.sql.gz
mysql -u root -p idra_local < idra.sql
```

※ Workbench でもインポートできますが、大容量（4.6GB）は**コマンドのほうが安全で確実**です。

---

# 🔒 ここまで安全を確保できた理由まとめ

| リスク要因            | 回避方法                    |
| ---------------- | ----------------------- |
| 本番DBを壊す          | dump専用ユーザー（SELECTのみ）で操作 |
| 誤更新・誤削除          | mysqldump は完全に読み取りコマンド  |
| 巨大SQLで失敗         | gzip圧縮で軽量化 + ローカルで復元    |
| ローカルの既存DB破壊      | `idra_local` という新規DBを使う |
| Workbenchのタイムアウト | コマンドラインを使用              |

---

# 🔧 必要であればさらに安全性を高めるオプション

### ✔ Dump前に本番を Read Lock しない

（`--single-transaction` を使っているのでロック不要）

### ✔ AWS のスナップショットを事前に取ってもらう

万一手順ミスをしても本番復旧可能。

---

# 📌 必要なら以下も作成できます

* あなたの AWS 環境に合わせた **完全な TeraTerm 操作コマンドセット**
* WinSCP・MySQL・Workbench の **画面付きステップガイド**
* あなたの環境に合わせた **自動化スクリプト（batファイル）**

必要ですか？

了解しました。
**「ローカルで idra.sql の取り込みが失敗したとき、何回でも安全に再実行できる手順」**を、
**MySQL Workbench / MySQL コマンドの両方**でわかりやすくまとめてご説明します。

---

# ✅ **ローカルで何度でも安全にやり直せる “正しい手順”**

ポイントは **「失敗したDBは全部削除 → 再作成 → 再取り込み」**
これだけで完全にクリーンな状態に戻ります。

---

# 🟩 **STEP 1：idra データベースを削除（DROP）する**

## ■ 方法① MySQL Workbench から削除

1. Workbench を開く
2. 左の「Schemas」にある **idra** を右クリック
3. **Drop Schema…（スキーマ削除）」** を選ぶ
4. 「Review SQL」画面が出るので **Drop Now** をクリック

これで DB 全体がきれいに消えます。

---

## ■ 方法② コマンドで削除（確実で安全）

Workbench の SQL Editor または Windows コマンドで以下を実行：

```sql
DROP DATABASE IF EXISTS idra;
```

---

# 🟦 **STEP 2：idra データベースを再作成する**

AWS本番と同じ設定で作成することが重要です。

MySQL Workbench の場合：

1. Workbench → 「Schemas」→ 右クリック → **Create Schema…**
2. 名前を **idra** にする
3. Default Collation（文字コード）は AWS に合わせる

例：

* utf8mb4_general_ci
* もしくは utf8mb4_0900_ai_ci（MySQL8の場合）

---

## コマンドで再作成する場合：

```sql
CREATE DATABASE idra CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
```

（※あなたのAWS環境に合わせて変更します）

---

# 🟧 **STEP 3：idra.sql（4.6GB）を取り込む**

### ■ 方法① Workbench で取り込む（GUI）

1. Workbench メニュー
   　→ **Server**
   　→ **Data Import**
2. 「Import from Self-Contained File」を選び
   　→ `idra.sql` を指定
3. 「Default Target Schema」で `idra` を選択
4. 一番下の **Start Import** を押す

⚠️ Workbench は **大容量に弱い** ので、
4GB以上は途中で止まることがあります。

成功率を高めるには「Workbench を再起動してから Import」が必須です。

---

# 🟥 **方法② MySQL コマンド（最も成功率が高い）**

こちらが 4.6GB のような大型SQLに一番確実です。

Windows コマンドプロンプトを開いて：

```
mysql -u root -p idra < C:\path\to\idra.sql
```

例：

```
mysql -u root -p idra < C:\backup\idra.sql
```

※パスはあなたの環境に合わせて変更。

---

# 🟨 **STEP 4：失敗しても同じ作業を繰り返せば安全**

失敗した場合は必ずこの順番です：

1. **DROP DATABASE idra;**
2. **CREATE DATABASE idra;**
3. **SQL取り込みを再実行**

これだけで **何回でもクリーンに再チャレンジできます。**

---

# ⚠️ **注意：絶対にやってはダメなこと**

### ✖ 本番AWSの RDS で DROP / IMPORT する

→ これは破壊行為になるため、Dump / Export 専用ユーザーで接続するべき。

### ✖ 部分的に失敗した DB を上書きインポート

→ データ不整合が起きて壊れます。

必ず **DROP → CREATE → IMPORT** の完全初期化手順を守ってください。

---

# 📘 もしよければ、あなたのローカル MySQL のバージョンと

idra の AWS 側の文字コードを教えていただければ、
**完全一致する CREATE DATABASE コマンドを作成します。**

必要なら Workbench の画面付きマニュアルも作成します。

了解です。
**「gzip を使わず、AWS の MySQL をそのまま `idra.sql` にダンプすると、どれくらい時間がかかるのか？」**
──これについて正確にお伝えします。

---

# 🟦 結論：**4.6GB の生ダンプなら “だいたい 5分〜20分” が一般的**

ただし、これは **AWS → EC2 → mysqldump → ファイル生成** の速度によって変わります。

### ▼ おおよその目安

| 処理方式                           | 想定時間      | 備考                      |
| ------------------------------ | --------- | ----------------------- |
| **EC2 内で mysqldump（gzipなし）**   | **5〜20分** | 一番早い & 安定。今回あなたが実施する方式。 |
| ローカルPCから AWS RDS に直で mysqldump | 20〜60分    | ネットワーク遅延が大きいので非推奨。      |
| gzipあり（EC2内）                   | 2〜10分     | 圧縮が速い場合さらに短縮。           |

---

# 🌟 今回あなたのケースの前提：

* EC2（AWS）に TeraTerm で SSH 接続
* RDS（MySQL）に対して **EC2側で mysqldump**
* `idra.sql` のサイズは **4.6GB**

この条件だと、**EC2はクラウド内高速ネットワーク**のため
**5〜20分以内で完了するケースがほとんど**です。

---

# 🟩 もっと精密な見積り（AWSの性能から計算）

### AWS の内部ネットワーク速度（参考値）

* 250〜800MB/秒 くらい
* ただし mysqldump は “SQLテキスト生成” がボトルネックになるため、
  実際は 4〜20MB/秒 程度になる。

### 4.6GB を 10MB/秒 で書き出すと…

```
4,600MB ÷ 10MB = 460秒（約7.6分）
```

### 最悪 3〜5MB/秒 の場合でも：

```
4,600MB ÷ 5MB ≒ 920秒（約15分）
```

→ 現実的に **7〜15分が中心**と考えてOK。

---

# 🟥 注意：Windows では 4GB 超えの gzip 解凍が安定しない

あなたが言われた通りです。

特に：

* Windows標準展開
* 7-Zipの古いバージョン
* セキュリティ制限下のPC

これらは **4GB以上のgzipを解凍中に失敗**することがよくあります。

そのため、**gzipなしのダンプ（idra.sql）にするのは非常に正しい判断**です。

---

# 🟧 安全なダンプコマンド（gzipなし）

AWS EC2 で実行：

```
mysqldump -h <RDSのホスト> -u <ユーザー> -p --set-gtid-purged=OFF idra > idra.sql
```

これだけで OK。

※ 大量データの場合は `--single-transaction` を付けるとより安全。

```
mysqldump --single-transaction --set-gtid-purged=OFF -h <HOST> -u <USER> -p idra > idra.sql
```

---

# 🟦 質問があればどうぞ！

* 実行した時の **画面の進行状況が見たい場合のコマンド**
* `mysqldump` が途中で止まった場合の対処
* `WinSCP` で 4.6GBを安全にダウンロードする設定
* 取り込み失敗を防ぐ MySQL の設定（バッファサイズなど）

必要があれば、**あなた専用の完全手順書（画面付き）**を作成します！


